-r local.txt
# Optional: real-browser responsive QA (scripts/responsive_check.py) — screenshots at every
# breakpoint, horizontal-overflow detection and hero-video gating checks.
# After installing run: python -m playwright install chromium
playwright>=1.50
